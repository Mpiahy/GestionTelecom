services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gestiontelecom-app
    ports:
      - "5050:5000"
    volumes:
      - .:/var/www/html
    networks:
      - laravel
    depends_on:
      - db

  webserver:
    image: nginx:latest
    container_name: gestiontelecom-webserver
    ports:
      - "8181:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - laravel
    depends_on:
      - app

  db:
    image: postgres:10
    container_name: gestiontelecom-db
    environment:
      POSTGRES_DB: telecom
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - laravel
    ports:
      - "5432:5432"

volumes:
  pgdata:

networks:
  laravel:
    driver: bridge
